<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Start Date:</strong>
  <%= @project.start_date %>
</p>

<p>
  <strong>End Date:</strong>
  <%= @project.end_date %>
</p>

<p>
  <strong>Word Count Goal:</strong>
  <%= @project.word_goal %>
</p>

<h2>Daily Word Counts</h2>
<%= render @project.daily_counts %>

<h2>Add a Wordcount:</h2>
<%= render 'daily_counts/form' %>

<%= link_to 'Edit', edit_project_path(@project) %> | <%= link_to 'Back', projects_path %>
